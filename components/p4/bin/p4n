#!/usr/bin/env bash
set -uo pipefail

p4_dir="$(dirname "$(readlink -f "$0")")"
source "$p4_dir/../lib/real-p4.sh"

p4bin="$(find-real-p4)" || true
if [[ -z "$p4bin" ]]; then
    echo "p4n: real p4 binary not found on PATH" >&2
    exit 127
fi

exec "$p4bin" "$@"

